import { BenchmarkDriver } from '../BenchmarkDriver';
import { BenchmarkOptions } from '../decorators';


//n const drivers: Map<string, BenchmarkDriver[]>

export function registrateBenchmark(benchmarkName: string, options: any) {
    
}